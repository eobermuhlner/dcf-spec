dcf_version: "1.0.0"
kind: component
profile: strict
name: Container
category: primitive
description: Flexible layout wrapper with directional support

props:
  direction: string
  align: string
  justify: string
  gap: string
  padding: string
  wrap: string
  condition: string

layout:
  display: flex
  direction:
    default: column
    below_sm: column

accessibility:
  role: none
  keyboard: false
